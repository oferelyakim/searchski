{
  "name": "@searchski/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Searchski domain model + deterministic, explainable scoring engine. An LLM never ranks.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "files": [
    "src"
  ],
  "comment": "golden/test run on plain Node via ../../scripts/ts-resolve.mjs, which maps TS-ESM '.js' import specifiers onto the .ts sources. That needs Node >=22.18 (native type stripping) but no native binaries — esbuild/tsx is blocked by OS policy on some machines in this project. golden:tsx keeps the tsx path for older Node.",
  "scripts": {
    "golden": "node --import ../../scripts/ts-resolve.mjs src/golden.ts",
    "test": "node --import ../../scripts/ts-resolve.mjs src/golden.ts",
    "golden:tsx": "tsx src/golden.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
